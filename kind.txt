I reviewed the SMTP core, SMTP admin, and the emailer send paths. I have not fixed anything yet.

Findings

Critical: custom SMTP has no explicit connection timeout in either the live send path or the test path, so a bad host/port/auth/firewall issue can block the whole request until the socket layer gives up. Thatâ€™s in smtp core:149 (line 149) and smtp core:295 (line 295). PHP mail feels instant because smtp core:132 (line 132) switches to isMail() and avoids the SMTP network handshake entirely.

Critical: your new fallback is synchronous inside the failure hook. After the primary send fails, smtp core:570 (line 570) immediately calls wp_mail() again at smtp core:583 (line 583) before the browser gets a response. If the primary SMTP is slow and fallback is also slow or wrong, one bad send becomes two long blocking sends.

Critical: the log table is written as pending before send, and only finalized later on success/failure hooks. See smtp core:492 (line 492) and smtp core:525 (line 525). So if the request hangs, gets aborted, or dies during SMTP, the row never gets updated and stays pending forever. That matches your symptom exactly.

High: the rate-limit flow is ordered incorrectly. You log on wp_mail at smtp core:67 (line 67) / smtp core:492 (line 492), then short-circuit later on pre_wp_mail at smtp core:97 (line 97). When the limit is hit, you also insert a second rate-limited log row at smtp core:621 (line 621). Result: orphaned pending entries and duplicate logging.

High: password storage is inconsistent. The setup wizard imports SMTP passwords raw at setup wizard:190 (line 190), but the SMTP sender always tries to decrypt them at smtp core:154 (line 154) and smtp core:407 (line 407). If the current config was ever imported or came from older storage, custom SMTP auth can break while PHP mail still works.

High: the SMTP logger keeps request-global state in single properties, which is not safe once multiple mails happen in one request. The shared state is at smtp core:17 (line 17), smtp core:27 (line 27), and smtp core:500 (line 500). Your emailer does bulk sends in plain loops at email admin:1655 (line 1655), email admin:3755 (line 3755), and email core:160 (line 160). That makes stale or mis-assigned log state much more likely.

Medium: the SMTP test is not testing the real production path. It creates a fresh PHPMailer directly at smtp core:287 (line 287) instead of exercising wp_mail() with the normal hooks at smtp core:57 (line 57). So even if the test passes, live sends can still fail differently.

Medium: the â€œLog Email Contentâ€ setting is mostly cosmetic. You always store the body at smtp core:510 (line 510), while the setting is only saved/displayed from smtp admin:992 (line 992) and smtp admin:2114 (line 2114). Thatâ€™s a privacy/security bug.

Why Youâ€™re Seeing This
Custom SMTP is doing blocking network work inside normal page requests, and your emailer sends one message at a time in those same requests. Once SMTP is slow, the browser waits; if you stop waiting or the request dies before the hooks run, the log row you inserted as pending never gets closed.

Notes
I didnâ€™t change code yet, and I couldnâ€™t do a live SMTP send from this sandboxed environment. I did confirm the files are syntactically valid with php -l.

If you want, Iâ€™ll fix this next in the right order: timeout + stale-pending/log sequencing first, fallback flow second, password/decrypt compatibility third, then the bulk-send path.




1:01 PM
im confused seriously, if you found out all these and the custom smtp is working on local server but not on live site. on local server i nput my correct live credentials and everything work perfectly. does it mean that all you pointed out here are still the issue or migration?
1:38 PM



Worked for 10s
No. It does not mean every issue I listed is the direct cause of the live-site failure.

What your test tells us is this:

If the same credentials work on your local machine but fail on the live server, then the most likely primary problem is environment-specific on the live host, not that your SMTP code is 100% fundamentally broken.

Most likely live-only causes:

Outbound SMTP ports like 587 or 465 are blocked by the host/firewall.
The live server cannot resolve or reach the SMTP hostname reliably.
TLS/certificate validation behaves differently on live.
The SMTP provider is rejecting the live server IP, but not your local network.
The live box is just timing out badly on socket connect/auth.
That said, the issues I found are still real, because they explain why the failure looks so ugly on live:

No SMTP timeout set:
On local, connection succeeds quickly, so you donâ€™t notice.
On live, if the host blocks or stalls SMTP, the request hangs for a long time.

Log written as pending before completion:
On local, success hook runs quickly and flips it to success.
On live, if the request stalls or dies, it stays pending.

Fallback runs synchronously:
On local, maybe you never hit fallback.
On live, if primary is slow/failing, fallback can make the wait even worse.

So the clean way to say it is:

Your module has real architectural bugs.
But the live-only breakage strongly suggests an additional server/network issue.
Those architectural bugs are making that server issue much harder to diagnose and much worse in UX.
About migration specifically:

The password/decryption issue is only likely if the live site is using an old/imported/saved password value.
If on local you typed the credentials fresh into the current settings UI, but live is using a previously imported/stored password, then yes, migration/storage mismatch could absolutely be part of it.
If both local and live were freshly entered the same way, then migration is less likely, and host/network becomes more likely.
So my honest read is:

Primary suspicion: live server connectivity / TLS / blocked SMTP.
Secondary suspicion: stored-password mismatch on live.
Definitely real bugs regardless: timeout, pending-log flow, fallback blocking.
If you want, I can do the next step very methodically:

inspect the port-test module and live send path,
tell you the exact checks to run on the live server,
then patch the code so even when live SMTP fails, it fails fast and logs correctly.



1:38 PM
you are right, my live server is having issue. so i tried the plugin on another live site and everything works well. so regarding your findings are they stilll true? no code yet
7:45 PM



Yes, most of them are still true.

The new information changes the root-cause diagnosis, not the code review. Since the plugin works on another live site, the original outage was primarily that server environment. But the code issues I flagged are still real because they affect resilience, logging accuracy, and how badly the plugin behaves when a host has SMTP trouble.

Still True

No explicit SMTP timeout in the live send and test paths. Thatâ€™s still a code issue in class-ofast-smtp.php (line 149) and class-ofast-smtp.php (line 295).
Logs are inserted as pending before send completion, so stalled/killed requests can leave stale rows behind. Thatâ€™s still true in class-ofast-smtp.php (line 507).
Fallback retries inside the same failing request, which can double the wait on bad hosts. Still true in class-ofast-smtp.php (line 571).
The SMTP test path does not exercise the exact normal wp_mail() production flow. Still true in class-ofast-smtp.php (line 287).
The â€œlog email contentâ€ setting is not actually enforced; body content is still always stored. Still true in class-ofast-smtp.php (line 510) versus class-ofast-smtp-admin.php (line 2115).
Still True, But Not Proven To Be This Incident

Shared state with current_log_id and last_email_args is still fragile for multiple mails in one request.
The emailer still sends synchronously in loops, so slow SMTP will scale badly on larger batches.
The rate-limit/logging order issue is still there, but only matters when the limit is actually hit.
Conditional

The password migration/decryption concern is still valid as a code smell, but it only matters if a site is using imported or legacy-stored credentials. That mismatch is between class-ofast-setup-wizard.php (line 194) and class-ofast-smtp.php (line 154).
So the short answer is: the findings still stand, but they should now be read as hardening and correctness issues, not â€œproof that the SMTP module canâ€™t work.â€ The module can work; it just doesnâ€™t fail cleanly when the server is unhealthy.












================================================================================
SMTP V2 — SESSION RECORD
Date: 2026-04-26
================================================================================

WHAT WAS FIXED IN THIS SESSION
--------------------------------

1. SMTP connection timeout — class-ofast-smtp.php
   Added Timeout = 10 in configure_phpmailer() (live send path)
   Added Timeout = 10 in ajax_test_smtp() (test send path)
   Done by Codex, verified correct.

2. Password encryption on wizard import — class-ofast-setup-wizard.php
   import_smtp_settings() now wraps password with Ofast_X_SMTP::encrypt_password()
   Added class-load guard before the static call. Done by Codex, verified correct.

3. Log body content setting enforced — class-ofast-smtp.php
   log_outgoing_email() now reads ofast_smtp_log_body_content option.
   Body stored only when setting is ON. Default OFF (privacy safe).
   Option name verified against admin file — exact match.

4. Stale-pending cleanup — class-ofast-smtp.php
   Codex put sweep inside cleanup_old_logs() which runs DAILY — wrong interval.
   FIXED: extracted sweep_stale_pending() on its own HOURLY cron event.
   Rows stuck pending >10 min marked failed with clear message.
   cleanup_old_logs() now only handles daily log-retention deletion.

5. Resend button extended — class-ofast-smtp-admin.php
   Resend was only on failed rows. Now also on success/sent/resent rows.
   Pending rows excluded — could duplicate an in-flight send.
   resend_email() writes status resent so log is unambiguous.
   Added RESENT badge (green) as a distinct visual status.


WHAT IS STILL UNTOUCHED (SMTP V3 SCOPE)
-----------------------------------------

1. ASYNC FALLBACK — mark_email_failed() in class-ofast-smtp.php
   Fallback retry still synchronous. Worst case now 20s with Timeout=10.
   Proper fix: persist failed args to DB, queue via WP Cron.
   Complexity: Medium-High. Plan carefully before touching.

2. SINGLETON STATE — current_log_id / last_email_args
   Safe in normal sequential PHP. Risk only on reentrant wp_mail() calls.
   Fix when a test suite is in place. Complexity: Medium.


VERDICT
--------
Module is solid and production-ready for healthy server environments.
Fails gracefully (not silently) on bad hosts since v2.
V3 work is architectural hardening — not breakage prevention.

================================================================================

